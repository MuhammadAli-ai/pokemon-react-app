// async await func that will make fetch call to url and return data
export async function getAllPokemon(url) {
  return new Promise((resolve, reject) => {
    fetch(url)
      .then((res) => res.json())
      .then((data) => {
        resolve(data);
      });
  });
}

// func inside loadingPokemon runs on mapped fetch urls arrays from fetch above and now fetch urls of each pokemon then set state for each pokemonData
export async function getPokemon(url) {
  return new Promise((resolve, reject) => {
    fetch(url)
      .then((res) => res.json())
      .then((data) => {
        resolve(data);
      });
  });
}
